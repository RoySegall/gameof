var fs = require('fs');

/**
 * Get all the plugins.
 *
 * @param modules
 *   The main module object.
 */
var getListPlugins = function(modules) {

  // Get all the modules except for this module.

  // Get their location.

  // Iterate over files and et the info from the annotation.

  // construct an express object.

};

/**
 * Get the information on the plugin.
 *
 * @param file
 *   The file path.
 */
var getPluginInfo = function(file) {
  // return an object wih the path info and the methods.

};

module.exports = {
  getListPlugins: getListPlugins,
  getPluginInfo: getPluginInfo
};
